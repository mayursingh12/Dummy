<%#= simple_form_for [@beer] do |f| %>
    <%#= f.input :name %>
    <%#= f.submit 'Save', class: 'btn btn-primary' %>
<%# end %>

<%= simple_form_for [@beer_category, @beer] do |f| %>
    <%= f.input :name %>
    <%= f.submit 'Save', class: 'btn btn-primary' %>
<% end %>