<%= simple_form_for [@beer_category, @beer] do |f| %>
    <%= f.input :name %>
    <%= f.input :price %>
    <%= f.input :description %>
    <%= f.input :pair_with %>
    <%= f.submit 'Save', class: 'btn btn-primary' %>
<% end %>